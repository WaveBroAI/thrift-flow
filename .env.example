# API Keys — proxy holds all provider keys; clients don't need them
OPENROUTER_API_KEY=sk-or-...
GROQ_API_KEY=gsk_...

# Optional: override config.yaml settings via env
# PROXY_HOST=0.0.0.0
# PROXY_PORT=8888
